# Server Port
PORT=8080

# Turso (SQLite) Connection Settings
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# Firebase Admin SDK Credentials
# Note: In production (Google Cloud Run), these can be injected via GCP Secret Manager
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
